# 🚀 Tendies — Пошаговый гайд по запуску на Solana (для новичков)

Этот файл проведёт тебя от «ничего нет» до «токен живой на stonkfun, награды
капают акциями каждые 30 минут». Делай строго по порядку. Если что-то
непонятно — остановись на этом шаге и спроси.

Обозначения:
- 🖥️ = терминал (приложение «Терминал» на Mac)
- 👻 = кошелёк Phantom
- 🌐 = браузер

> ⚠️ ГЛАВНОЕ ПРАВИЛО: любой адрес, который ты копируешь (минт токена, USDC,
> xStocks), проверяй на **solscan.io** перед тем, как отправлять туда деньги.
> Опечатка в адресе = деньги ушли незнакомцу, вернуть нельзя. И никогда не
> вставляй свой seed/секретный ключ на сайты.

---

## ЧАСТЬ 0. Что приготовить (20 минут)

### 0.1. Кошелёк создателя — он же казна
👻 Поставь **Phantom** (phantom.app) и заведи **новый** кошелёк — не тот, где
твои личные деньги. Сид-фразу на бумагу.

Этот кошелёк делает две работы сразу: с него запускается токен, и на него же
StonkFun **автоматически** пересылает долю создателя от торговой комиссии.
Отдельный адрес-получатель указать нельзя — платформа всегда платит именно
кошельку-создателю (проверено по их API: «доля создателя форвардится на
кошелёк создателя автоматически, claim'ить подписью нечего»). Поэтому казна
и создатель — один и тот же адрес, иначе комиссию пришлось бы переливать
руками каждый раз.

### 0.2. SOL на комиссии
Купи/переведи **0.3–0.5 SOL** на этот кошелёк. С огромным запасом: сам запуск
через LaunchLab стоит **0 SOL комиссии платформы**, только рента —
~0.012 SOL (+0.003, если делаешь dev buy). Остальное уйдёт на транзакции
кипера и создание токен-аккаунтов получателям.

### 0.3. Секретный ключ для кипера
Phantom → Настройки → «Показать секретный ключ» (строка base58). Это ключ от
казны — им кипер подписывает выплаты. **Никуда, кроме переменных Railway, его
не вставляй.**

> ⚠️ Раз кошелёк создателя = казна, он становится горячим. Держи на нём только
> рабочий остаток, а накопленное сверх нужного выводи на холодный адрес.

---

## ЧАСТЬ 1. Запуск токена на stonkfun (15 минут)

### 1.1. Зайти на лаунчпад
🌐 https://www.stonkfun.xyz/ → **Connect wallet** → подключи Phantom
(кошелёк из 0.1).

### 1.2. Создать монету
Заполняешь:
- **Name:** `Tendies`
- **Ticker:** `TENDIE`
- **Image:** логотип (см. `BRAND_PROMPTS.md` — там промт для генерации)
- **Description:** «Hold the bag. Get the tendies. Треasury платит холдерам
  токенизированными акциями (OpenAI · TSLAx · NVDAx · SPCXx) каждые 30 минут.»
- **Quote token:** вот здесь весь смысл. Вкладка **PreStocks** → **OPENAI**
  (`PreweJYECqtQwBtpxHL171nL2K6umo692gTm7Q3rpgF`). ⚠️ В списке два OPENAI —
  бери тот, что в категории **PreStocks** с этим минтом, а не «T-OpenAI» от
  Tessera (`oPAi…`, ликвидность вдвое меньше). Квота ровно одна и
  **навсегда** — поменять после создания пула нельзя. Пара TENDIE/OPENAI сама
  рассказывает историю продукта, монета попадает в категорию PreStocks, и
  выплаты в OpenAI потом идут вообще без свопа, потому что комиссия капает
  в том числе в OPENAI.
- **Режим:** **Fee coin** (`standard`), НЕ «Reward coin». Reward-режим платит
  холдерам сам, но только одной акцией и без доли создателя — а нам нужна
  доля создателя, чтобы кипер платил тремя акциями на выбор.
- **Тариф пула:** бери **2%**. На 1% создателю идёт 0.5% с каждой сделки, на
  2% — **1.5%**. Разница берётся из комиссии трейдера, доля платформы не
  меняется. Для нас комиссия создателя — это весь бюджет наград, так что
  холдеры получают втрое больше акций.

### 1.3. Записать минт
После создания у монеты появится адрес — **mint address**. Скопируй его,
он понадобится дважды (сайт + кипер). Проверь его на solscan.io.

### 1.4. Комиссия в казну — настраивать нечего
Доля создателя (1.5% с каждой сделки на тарифе 2%) приходит на
кошелёк-создатель **автоматически**. Поля «получатель» на платформе нет и
claim'ить ничего не нужно. Раз по шагу 0.1 создатель и есть казна — деньги
уже там, где их ждёт кипер.

Комиссия приходит **обоими токенами пары**: частью в TENDIE, частью в OPENAI.
Кипер это умеет — он свопает накопленное в ту акцию, которую выбрал холдер.

---

## ЧАСТЬ 2. Адреса акций (10 минут)

Награды платятся токенизированными акциями на Solana: OpenAI — через
PreStocks (pre-IPO экспозиция через SPV), остальные — xStocks Backed Finance.

Четыре минта уже выписаны и сверены с реестром пар StonkFun — они лежат в
`lib/stocks.ts`, переписывать ничего не надо:

| Акция | Тикер | Минт |
| --- | --- | --- |
| OpenAI | OPENAI | `PreweJYECqtQwBtpxHL171nL2K6umo692gTm7Q3rpgF` |
| Tesla | TSLAx | `XsDoVfqeBukxuZHWhdvWHBhgEHjGNst4MLodqsJHzoB` |
| NVIDIA | NVDAx | `Xsc9qvGR1efVDFGLrVsmkzv3qi45LTBjeUKSPmx9qEh` |
| SpaceX | SPCXx | `Xs3oZwbHvqis4NYcf4YKWmEia2eC84wSiVrcYcTqpH8` |

Это **Token-2022** минты (OPENAI — 9 знаков, xStocks — 8), не классический SPL —
кипер спрашивает у сети, какая программа владеет минтом, потому что адрес
токен-аккаунта у них считается по-разному.

> ⚠️ В списке пар StonkFun 21 неоднозначный символ — есть поддельные STONK,
> OPENAI, WBTC. Сверяй **по минту**, не по тикеру. Их API так и говорит:
> `selectBy: "mint"`.

---

## ЧАСТЬ 3. Оживить сайт (5 минут)

### 3.1. Вписать адреса
Открой `lib/config.ts` и заполни:
- `TENDIE_MINT` = минт из шага 1.3 → сайт начнёт читать баланс из кошелька
- `BUY_TENDIE_URL` = ссылка на страницу монеты на stonkfun → заработают кнопки
  «Buy TENDIE» на дашборде и в Trade
- `TREASURY_WALLET` = адрес кошелька-создателя из 0.1 (он же казна)

`lib/stocks.ts` уже заполнен — минты сверены, трогать не нужно.

### 3.2. Быстрый RPC (важно)
Публичный RPC Solana режет запросы. Заведи ключ на Helius или QuickNode и
добавь в переменные окружения Vercel **`SOLANA_RPC`** (без префикса
`NEXT_PUBLIC_`!).

⚠️ Всё, что названо `NEXT_PUBLIC_*`, Next вшивает в браузерный бандл — ключ
увидит любой посетитель через просмотр кода и сожжёт твою квоту. Поэтому
адрес с ключом живёт только на сервере, а браузер ходит через прокси
`/api/rpc`, который пропускает лишь те методы чтения, что нужны приложению.

### 3.3. Опубликовать
🖥️
```bash
cd /Users/johnbuz/Desktop/PROJECTS/robinhood
npx vercel --prod
```

---

## ЧАСТЬ 4. Кипер на Railway (20 минут)

Кипер — бот, который каждые 30 минут: смотрит, кто держит TENDIE → меняет
накопленную комиссию на выбранные акции → раздаёт их холдерам пропорционально.
Без него награды не поедут.

### 4.1. Загрузить проект на GitHub
🖥️
```bash
cd /Users/johnbuz/Desktop/PROJECTS/robinhood
git add . && git commit -m "Tendies on Solana"
git push
```

### 4.2. Создать сервис
🌐 https://railway.app → войти через GitHub → **New Project** → **Deploy from
GitHub repo** → твой репозиторий. В **Settings → Root Directory** впиши
`keeper`.

### 4.3. Прописать переменные
🌐 Вкладка **Variables**:

| Имя | Значение |
| --- | --- |
| `TENDIE_MINT` | минт из шага 1.3 |
| `TREASURY_SECRET_KEY` | секретный ключ казны (base58) из 0.3 |
| `PAYOUT_MINTS` | готовая строка — см. под таблицей |
| `FEE_MINT` | `PreweJYECqtQwBtpxHL171nL2K6umo692gTm7Q3rpgF` — квота пула (OPENAI); без этого кипер думает, что комиссия приходит в USDC |
| `SOLANA_RPC` | твой Helius/QuickNode endpoint |
| `EXCLUDE_ACCOUNTS` | адреса пула/кривой stonkfun и самой казны, через запятую |

Перпы включаются сами, как только кипер живой — отдельных переменных не
нужно. Казна откладывает 10% каждой эпохи в **резерв** (до 20% от баланса) —
из него платятся выигрыши, в него уходят проигрыши и funding. Лимиты по
умолчанию: плечо до 10×, одна позиция ≤ 50% резерва, весь открытый интерес
≤ 200% резерва, funding 0.05% каждые 8 ч, ликвидация при −95% маржи. Пока
резерв нулевой (первые эпохи), открыть ничего нельзя — это нормально.
Менять — через `PERPS_*` (см. `keeper/README.md`). `PERPS_ENABLED=false`
ставит открытие на паузу, не трогая уже открытые позиции.

`PAYOUT_MINTS` копируй целиком, одной строкой без пробелов:

```
OPENAI:PreweJYECqtQwBtpxHL171nL2K6umo692gTm7Q3rpgF,TSLAx:XsDoVfqeBukxuZHWhdvWHBhgEHjGNst4MLodqsJHzoB,NVDAx:Xsc9qvGR1efVDFGLrVsmkzv3qi45LTBjeUKSPmx9qEh,SPCXx:Xs3oZwbHvqis4NYcf4YKWmEia2eC84wSiVrcYcTqpH8
```

`EXCLUDE_ACCOUNTS` — обязательно. Иначе пул ликвидности будет получать награды
наравне с живыми холдерами и съест их долю.

### 4.4. Сначала — сухой прогон
Пока не задан `TREASURY_SECRET_KEY`, кипер работает в **DRY-RUN**: считает
полный план раздачи и пишет в логи, что *отправил бы*, но не отправляет.
Запусти так на один-два цикла и почитай **Logs** — увидишь число холдеров и
разбивку по акциям. Только потом добавляй ключ.

### 4.5. Проверить
🌐 Открой `https://твой-сервис.up.railway.app/status` — там живые числа:
холдеры, накопленная комиссия, когда следующая эпоха.

---

## ЧАСТЬ 5. Безопасность (ДО крупных денег)

- **Ключ казны живёт только в Railway.** Не в репозитории, не в переписке.
- **Держи в казне ровно рабочий остаток.** Это горячий кошелёк: всё, что в нём
  лежит, теоретически уводится вместе с ключом.
- **Проверь каждый минт на solscan** ещё раз перед первой реальной раздачей.
- **Первую эпоху сделай маленькой** — пусть раздастся немного, посмотри
  транзакции в эксплорере, убедись, что людям пришло.

---

## Порядок одним взглядом

1. Один кошелёк (создатель = казна) + 0.3–0.5 SOL (Часть 0)
2. Создать TENDIE на stonkfun: квота **OPENAI** (PreStocks, минт PreweJYECqtQwBtpxHL171nL2K6umo692gTm7Q3rpgF), режим **Fee coin**, тариф
   **2%**, записать минт (Часть 1)
3. Минты OPENAI / TSLAx / NVDAx / SPCXx уже в `lib/stocks.ts` (Часть 2)
4. Вписать адреса в `lib/config.ts` и `lib/stocks.ts`, задеплоить сайт (Часть 3)
5. Railway: кипер в DRY-RUN → посмотреть план → включить ключ казны (Часть 4)
6. Безопасность: остаток в казне, проверка минтов, маленькая первая эпоха (Часть 5)

Застрял на любом шаге — пришли номер шага и текст ошибки, разберём.
