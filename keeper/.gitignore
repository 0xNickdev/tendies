node_modules/

# env / secrets — the treasury key lives here, never commit it
.env
.env.*

# keeper runtime state — the ledger, choices, epoch journal, holder cache
data/
data-e2e/
data-test*/

!.env.example
